<%= form_with(model: following_user) do |form| %>
  <% if following_user.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(following_user.errors.count, "error") %> prohibited this following_user from being saved:</h2>

      <ul>
        <% following_user.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :user_id, style: "display: block" %>
    <%= form.text_field :user_id %>
  </div>

  <div>
    <%= form.label :following_user_id, style: "display: block" %>
    <%= form.number_field :following_user_id %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
