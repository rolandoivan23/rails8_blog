<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for(:title) || "Blog" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <%# Enable PWA manifest for installable apps (make sure to enable in config/routes.rb too!) %>
    <%#= tag.link rel: "manifest", href: pwa_manifest_path(format: :json) %>

    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/icon.png">

    <%# Includes all stylesheet files in app/assets/stylesheets %>
    <%= stylesheet_link_tag :app, "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body>
    
   
    <!-- Header Section -->
    <header>
        <h1>Modern Blog</h1>
        <nav>
            <ul>
                <li><%= link_to 'Home', root_path %></li>
                <li><%= link_to 'People', users_path %></li>
                <li><%= link_to 'Categories', categories_path %></li>
                <li><%= link_to 'About', contact_index_path %></li>
                <li><%= link_to 'Contact', contact_contact_path %></li>
            </ul>
        </nav>
        
    </header>

    
    <!-- Main Content Wrapper -->
    <div class="content-wrapper">
        <!-- Left Sidebar - User Information -->
        <aside class="left-sidebar">
        <% if authenticated? %>
            <div class="user-profile">
                <% if current_user.avatar.attached? %>
                    <%=link_to user_path(current_user) do %>
                        <%= image_tag current_user.avatar, class: "profile-image" %>
                    <%end%>
                <% else %>
                    <img class="profile-image" src="https://www.claudeusercontent.com/api/placeholder/100/100" alt="User Profile">
                <% end %>
                <h3 class="user-name"><%= current_user.email_address %></h3>
                
                <p class="user-bio">
                    <% if current_user.presentation %>
                        <%= current_user.presentation %>
                    <% else %>
                        Web designer, photographer, and coffee enthusiast :)
                    <% end %>
                </p>
                
            </div>
        
            <div class="user-stats">
                <div class="stat">
                    <div class="stat-number"><%= current_user.posts.count %></div>
                    <div class="stat-label">Posts</div>
                </div>
                <div class="stat">
                    <div class="stat-number"><%= current_user.followers.size %></div>
                    <div class="stat-label">Followers</div>
                </div>
                <div class="stat">
                    <div id="following-counter" class="stat-number"><%= current_user.following_users.size %></div>
                    <div class="stat-label">Following</div>
                </div>
                <%= button_to "Sign out", session_path, method: :delete if authenticated? %>
                <%= link_to "New Post", new_post_path, class: 'new-post-btn', style: 'height: 45px;' %>
            </div>
        <% else %>
            <%= link_to 'Login', new_session_path, class: 'button' %>
            <%= link_to 'Sign Up', new_user_path, class: 'button' %>
        <% end %>
        </aside>
        
        <main class="main-content">
          <article class="post">
            <%= yield %>
          </article>  
        </main>

       

        <!-- Right Sidebar - Related Content and News -->
        <aside class="right-sidebar">
            <div class="related-content">
                <h3 class="section-title">Related Posts</h3>
                <div class="related-posts">
                    <% Post.last(3).each do |post| %>
                        <div class="related-post">
                            <%= image_tag post.hero_image, class: 'related-post-img', alt: 'Related Post' if post.hero_image.attached? %>
                            <div class="related-post-content">
                                <h4 class="related-post-title"> <%= post.title %> </h4>
                                <div class="related-post-date"><%= time_tag post.created_at, "data-local": "time", "data-format": "%Y %B" %></div>
                            </div>
                        </div>
                    <% end %>
                    
                </div>
            </div>

            <div class="news">
                <h3 class="section-title">Latest News</h3>
                <div class="news-items">
                    <% Bulletin.all.each do |bulletin| %>
                        <div class="news-item">
                            <h4 class="news-title"><%= bulletin.title %></h4>
                            <div class="news-source"><%= bulletin.source %> - <%= time_tag bulletin.created_at, "data-local": "time", "data-format": "%Y %B" %></div>
                        </div>
                    <% end %>
                </div>
            </div>
        </aside>
    </div>

    <!-- Footer Section -->
    <footer>
        <div class="footer-content">
            <div class="footer-section">
                <h3 class="footer-title">About</h3>
                <p>Modern Blog is a platform dedicated to sharing insights and news about web development, design, and technology.</p>
            </div>
            <div class="footer-section">
                <h3 class="footer-title">Quick Links</h3>
                <ul class="footer-links">
                    <li><a href="#">Home</a></li>
                    <li><a href="#">Categories</a></li>
                    <li><a href="#">About Us</a></li>
                    <li><a href="#">Contact</a></li>
                </ul>
            </div>
            <div class="footer-section">
                <h3 class="footer-title">Connect</h3>
                <div class="footer-social">
                    <a href="#" class="social-icon">FB</a>
                    <a href="#" class="social-icon">TW</a>
                    <a href="#" class="social-icon">IG</a>
                    <a href="#" class="social-icon">LI</a>
                </div>
            </div>
        </div>
        <div class="copyright">
            &copy; 2025 Modern Blog. All rights reserved.
        </div>
    </footer>
  </body>
</html>
